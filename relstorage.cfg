[buildout]
extends = 
    base.cfg
    rsync.cfg

[instance]
recipe = plone.recipe.zope2instance
relative-paths = true
eggs +=
    RelStorage
    psycopg2

shared-blob = off
rel-storage =
    keep-history true
    blob-dir var/blobcache
    shared-blob-dir false
    type postgresql
    host devel.interne.imio.be
    dbname zodbuser
    user zodbuser
    password zeeGeer7

[versions]
RelStorage = 1.5.1
psycopg2 = 2.6
